fun main() {
    for (i in 1..10) {
        for (j in 1..10) {
            print("$i * $j = ${i * j}\t") // \t - табуляция
        }
        println() // Переход на новую строку после каждой строки таблицы
    }
}
